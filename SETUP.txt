╔══════════════════════════════════════════════════════════════════════════════╗
║                  DISEASE SURVEILLANCE AI AGENT SYSTEM                        ║
║                         Complete Setup Guide                                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
  TABLE OF CONTENTS
═══════════════════════════════════════════════════════════════════════════════
1. Prerequisites
2. Azure Resources Setup
3. Database Setup
4. Backend Setup (Python with UV)
5. Frontend Setup (Next.js)
6. Environment Variables Configuration
7. Running the Application
8. Testing the System
9. Troubleshooting
10. Architecture Overview

═══════════════════════════════════════════════════════════════════════════════
  1. PREREQUISITES
═══════════════════════════════════════════════════════════════════════════════

Required Software:
  ✓ Python 3.11 or higher
  ✓ Node.js 18+ and npm
  ✓ UV (Python package installer) - https://github.com/astral-sh/uv
  ✓ Git
  ✓ SQL Server (Azure SQL or Local)
  ✓ Azure Account with active subscription

Optional Tools:
  ✓ Visual Studio Code
  ✓ Azure Data Studio or SQL Server Management Studio
  ✓ Postman (for API testing)

═══════════════════════════════════════════════════════════════════════════════
  2. AZURE RESOURCES SETUP
═══════════════════════════════════════════════════════════════════════════════

You need to create the following Azure resources:

A. Azure AI Foundry Hub & Project
   ────────────────────────────────────
   1. Go to Azure Portal → Create "Azure AI Foundry"
   2. Create a new Hub:
      - Choose your subscription
      - Create/select resource group
      - Choose region (e.g., East US)
   
   3. Create a new Project within the Hub:
      - Give it a meaningful name (e.g., "disease-surveillance-ai")
      - Note the PROJECT CONNECTION STRING (needed for .env)
   
   4. Deploy a Model:
      - Go to Project → Deployments
      - Deploy "gpt-4" or "gpt-4o" model
      - Note the DEPLOYMENT NAME (needed for .env)
   
   📝 Copy these values:
      - AZURE_AI_AGENT_PROJECT_CONNECTION_STRING
      - Model Deployment Name

B. Azure SQL Database
   ────────────────────────────────────
   1. Go to Azure Portal → Create "SQL Database"
   2. Configuration:
      - Server: Create new or use existing
      - Database name: disease_surveillance_db
      - Pricing tier: Basic/Standard (dev) or Premium (production)
      - Authentication: SQL authentication
   
   3. Firewall Settings:
      - Add your client IP address
      - Allow Azure services access
   
   4. Get connection string:
      - Go to Database → Connection strings
      - Copy the ADO.NET connection string
      - Format: Driver={ODBC Driver 17 for SQL Server};Server=tcp:yourserver.database.windows.net,1433;Database=disease_surveillance_db;Uid=yourusername;Pwd=yourpassword;Encrypt=yes;TrustServerCertificate=no;
   
   📝 Copy this value:
      - DB_CONNECTION_STRING

C. Azure Storage Account
   ────────────────────────────────────
   1. Go to Azure Portal → Create "Storage Account"
   2. Configuration:
      - Performance: Standard
      - Replication: LRS (dev) or GRS (production)
   
   3. Create a container:
      - Name: "surveillance-reports"
      - Public access level: Private
   
   4. Get connection string:
      - Go to Access keys
      - Copy "Connection string" from key1
   
   📝 Copy this value:
      - AZURE_STORAGE_CONNECTION_STRING

D. Bing Search API (Optional but Recommended)
   ────────────────────────────────────
   1. Go to Azure Portal → Create "Bing Search v7"
   2. Choose your subscription and resource group
   3. Select pricing tier (F0 for free trial)
   4. Copy one of the API keys
   
   5. Create Bing Connection in AI Project:
      - Go to AI Project → Connections
      - Add Bing Search connection
      - Note the CONNECTION NAME
   
   📝 Copy these values:
      - BING_SEARCH_API_KEY
      - BING_CONNECTION_NAME

═══════════════════════════════════════════════════════════════════════════════
  3. DATABASE SETUP
═══════════════════════════════════════════════════════════════════════════════

Step 1: Connect to your Azure SQL Database
──────────────────────────────────────────────
Using Azure Data Studio or SQL Server Management Studio:
  - Server: yourserver.database.windows.net
  - Authentication: SQL Login
  - Username: your_admin_username
  - Password: your_admin_password
  - Database: disease_surveillance_db

Step 2: Create Database Tables
──────────────────────────────────────────────
Execute the following SQL scripts in order:

1. Create Tables:
   File: backend/sql/create_surveillance_tables.sql
   
   Open the file and execute the entire script. This creates:
   - hospital_surveillance_data
   - social_media_surveillance_data
   - environmental_surveillance_data
   - pharmacy_surveillance_data
   - anomaly_detections
   - outbreak_predictions
   - surveillance_alerts
   - surveillance_reports
   - agent_thinking_logs
   - chat_sessions

2. Create Stored Procedures (Optional):
   File: backend/sql/create_stored_procedures.sql
   Execute this for helper stored procedures

3. Load Sample Data (Optional for Testing):
   File: backend/sql/sample_data.sql
   Execute this to populate tables with test data

Step 3: Verify Database Setup
──────────────────────────────────────────────
Run this query to verify all tables are created:

SELECT TABLE_NAME 
FROM INFORMATION_SCHEMA.TABLES 
WHERE TABLE_TYPE = 'BASE TABLE'
ORDER BY TABLE_NAME;

Expected result: 10 tables listed

═══════════════════════════════════════════════════════════════════════════════
  4. BACKEND SETUP (Python with UV)
═══════════════════════════════════════════════════════════════════════════════

Step 1: Install UV (if not already installed)
──────────────────────────────────────────────
Windows (PowerShell):
  powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

macOS/Linux:
  curl -LsSf https://astral.sh/uv/install.sh | sh

Verify installation:
  uv --version

Step 2: Navigate to Backend Directory
──────────────────────────────────────────────
cd disease-surveillance-ai/backend

Step 3: Create Python Virtual Environment with UV
──────────────────────────────────────────────
# Create virtual environment
uv venv

# Activate virtual environment
# Windows PowerShell:
.\.venv\Scripts\activate

# Windows CMD:
.venv\Scripts\activate.bat

# macOS/Linux:
source .venv/bin/activate

Step 4: Install Dependencies using UV
──────────────────────────────────────────────
# Install all required packages
uv pip install -r requirements.txt

This installs:
  - FastAPI & Uvicorn (API server)
  - Semantic Kernel (Agent orchestration)
  - Azure AI Projects SDK
  - Azure Identity & Storage
  - PyODBC (Database connectivity)
  - Pandas, NumPy (Data processing)
  - PyTorch, scikit-learn (ML models)
  - Streamlit (Developer dashboard)
  - And more...

Step 5: Install ODBC Driver (if not installed)
──────────────────────────────────────────────
For database connectivity, you need ODBC Driver 17 or 18:

Windows:
  Download from: https://learn.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server
  Install "ODBC Driver 17 for SQL Server" or "ODBC Driver 18"

macOS:
  brew install microsoft-odbc-sql-server-driver

Linux (Ubuntu/Debian):
  curl https://packages.microsoft.com/keys/microsoft.asc | sudo apt-key add -
  curl https://packages.microsoft.com/config/ubuntu/$(lsb_release -rs)/prod.list | sudo tee /etc/apt/sources.list.d/mssql-release.list
  sudo apt-get update
  sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17

Step 6: Create Backend .env File
──────────────────────────────────────────────
Create a file named .env in the backend/ directory:

# Copy from .env.example
cp .env.example .env

# Or create manually with this content:

──────────────────── .env file content ────────────────────
# Azure AI Configuration
AZURE_AI_AGENT_PROJECT_CONNECTION_STRING=your_project_connection_string_here
AZURE_AI_AGENT_MODEL_DEPLOYMENT_NAME=gpt-4

# Database Configuration
DB_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=tcp:yourserver.database.windows.net,1433;Database=disease_surveillance_db;Uid=yourusername;Pwd=yourpassword;Encrypt=yes;TrustServerCertificate=no;

# Azure Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=youraccount;AccountKey=yourkey;EndpointSuffix=core.windows.net

# Bing Search API (Optional)
BING_SEARCH_API_KEY=your_bing_api_key
BING_CONNECTION_NAME=your_bing_connection_name

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging
LOG_LEVEL=INFO
────────────────────────────────────────────────────────────

⚠️  IMPORTANT: Replace ALL placeholder values with your actual Azure credentials!

Step 7: Test Backend Installation
──────────────────────────────────────────────
# Test database connection
python main.py --test-connection

# Test plugin initialization
python main.py --test-plugins

# Test agent definitions
python main.py --test-agents

Expected output: ✅ Success messages for each test

═══════════════════════════════════════════════════════════════════════════════
  5. FRONTEND SETUP (Next.js)
═══════════════════════════════════════════════════════════════════════════════

Step 1: Navigate to Frontend Directory
──────────────────────────────────────────────
cd disease-surveillance-ai/frontend

Step 2: Install Node.js Dependencies
──────────────────────────────────────────────
npm install

This installs:
  - Next.js 15 (React framework)
  - React 18 (UI library)
  - Tailwind CSS (Styling)
  - Radix UI components
  - Plotly, Chart.js (Visualizations)
  - And more...

Step 3: Create Frontend .env.local File
──────────────────────────────────────────────
Create a file named .env.local in the frontend/ directory:

──────────────────── .env.local file content ────────────────────
# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Application Settings
NEXT_PUBLIC_APP_NAME=Disease Surveillance AI
NEXT_PUBLIC_APP_DESCRIPTION=Proactive AI-Powered Disease Outbreak Detection and Prevention
────────────────────────────────────────────────────────────

⚠️  Note: If you deploy backend on different host/port, update NEXT_PUBLIC_API_URL

Step 4: Build Frontend (Optional)
──────────────────────────────────────────────
# For production build
npm run build

# For development, skip this step

═══════════════════════════════════════════════════════════════════════════════
  6. ENVIRONMENT VARIABLES CONFIGURATION - DETAILED GUIDE
═══════════════════════════════════════════════════════════════════════════════

HOW TO GET EACH ENVIRONMENT VARIABLE:
────────────────────────────────────────────────────────────

A. AZURE_AI_AGENT_PROJECT_CONNECTION_STRING
   ─────────────────────────────────────────
   Location: Azure AI Foundry Project
   Steps:
   1. Go to https://ai.azure.com
   2. Select your project
   3. Click "Settings" → "Project properties"
   4. Copy "Connection string"
   
   Format: Endpoint=https://yourproject.api.azureml.ms;SubscriptionId=xxx;ResourceGroup=xxx;ProjectName=xxx

B. AZURE_AI_AGENT_MODEL_DEPLOYMENT_NAME
   ─────────────────────────────────────────
   Location: Azure AI Foundry Project → Deployments
   Steps:
   1. Go to your AI Project
   2. Click "Deployments"
   3. Find your GPT-4 deployment
   4. Copy the "Deployment name" (e.g., "gpt-4" or "gpt-4o")

C. DB_CONNECTION_STRING
   ─────────────────────────────────────────
   Location: Azure SQL Database
   Steps:
   1. Go to Azure Portal
   2. Navigate to your SQL Database
   3. Click "Connection strings" (left menu)
   4. Copy "ADO.NET (SQL authentication)"
   5. Replace {your_password} with actual password
   6. Change to ODBC format:
      Driver={ODBC Driver 17 for SQL Server};Server=tcp:yourserver.database.windows.net,1433;Database=disease_surveillance_db;Uid=yourusername;Pwd=yourpassword;Encrypt=yes;TrustServerCertificate=no;

D. AZURE_STORAGE_CONNECTION_STRING
   ─────────────────────────────────────────
   Location: Azure Storage Account
   Steps:
   1. Go to Azure Portal
   2. Navigate to your Storage Account
   3. Click "Access keys" (left menu under Security + networking)
   4. Click "Show" on key1
   5. Copy "Connection string"
   
   Format: DefaultEndpointsProtocol=https;AccountName=xxx;AccountKey=xxx;EndpointSuffix=core.windows.net

E. BING_SEARCH_API_KEY (Optional)
   ─────────────────────────────────────────
   Location: Azure Bing Search Resource
   Steps:
   1. Go to Azure Portal
   2. Navigate to your Bing Search resource
   3. Click "Keys and Endpoint" (left menu)
   4. Copy "KEY 1" or "KEY 2"

F. BING_CONNECTION_NAME (Optional)
   ─────────────────────────────────────────
   Location: Azure AI Project → Connections
   Steps:
   1. Go to Azure AI Foundry
   2. Select your project
   3. Click "Connected resources" or "Connections"
   4. Find Bing Search connection
   5. Copy the connection name (e.g., "bing-search-connection")

═══════════════════════════════════════════════════════════════════════════════
  7. RUNNING THE APPLICATION
═══════════════════════════════════════════════════════════════════════════════

You need to run THREE components:

┌──────────────────────────────────────────────────────────────────────────────┐
│ Component 1: Backend API (Required)                                         │
└──────────────────────────────────────────────────────────────────────────────┘

Terminal 1 - Backend API:
──────────────────────────────────────────────
cd disease-surveillance-ai/backend

# Activate virtual environment (if not already activated)
# Windows:
.\.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# Start FastAPI server
python main.py

Expected Output:
╔══════════════════════════════════════════════════════════════╗
║   Disease Surveillance AI Agent System                      ║
║   Starting API Server...                                     ║
║                                                              ║
║   API: http://localhost:8000                                ║
║   Docs: http://localhost:8000/docs                          ║
║   Health: http://localhost:8000/health                      ║
╚══════════════════════════════════════════════════════════════╝

✓ API is ready when you see: "Application startup complete"

Test API health:
  Open browser: http://localhost:8000/health
  Or: curl http://localhost:8000/health

Interactive API docs:
  Open browser: http://localhost:8000/docs

┌──────────────────────────────────────────────────────────────────────────────┐
│ Component 2: Frontend Web App (Required)                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Terminal 2 - Frontend:
──────────────────────────────────────────────
cd disease-surveillance-ai/frontend

# Start Next.js development server
npm run dev

Expected Output:
  ▲ Next.js 15.x.x
  - Local:        http://localhost:3000
  - Environments: .env.local

  ✓ Ready in 2.5s

✓ Frontend is ready when you see: "Ready in X.Xs"

Access the application:
  Open browser: http://localhost:3000

Available pages:
  - http://localhost:3000/          (Chat Interface)
  - http://localhost:3000/dashboard (System Dashboard)
  - http://localhost:3000/alerts    (Alerts Management)
  - http://localhost:3000/anomalies (Anomaly Detection)
  - http://localhost:3000/predictions (Outbreak Predictions)
  - http://localhost:3000/reports   (Generated Reports)

┌──────────────────────────────────────────────────────────────────────────────┐
│ Component 3: Streamlit Dashboard (Optional - For Developers)                │
└──────────────────────────────────────────────────────────────────────────────┘

Terminal 3 - Streamlit Dashboard:
──────────────────────────────────────────────
cd disease-surveillance-ai/backend

# Activate virtual environment (if not already activated)
# Windows:
.\.venv\Scripts\activate
# macOS/Linux:
source .venv/bin/activate

# Start Streamlit dashboard
streamlit run streamlit_app.py

Expected Output:
  You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.x.x:8501

✓ Dashboard is ready when browser opens automatically

Access developer dashboard:
  Open browser: http://localhost:8501

Features:
  - System overview and metrics
  - Chat interface for testing
  - Anomaly visualization
  - Prediction charts
  - Alert management
  - Thinking logs viewer
  - Data source monitoring

═══════════════════════════════════════════════════════════════════════════════
  8. TESTING THE SYSTEM
═══════════════════════════════════════════════════════════════════════════════

A. Test Backend Independently
   ─────────────────────────────────────────
   
   1. Health Check:
      curl http://localhost:8000/health
   
   2. Get System Status:
      curl http://localhost:8000/api/surveillance/status
   
   3. Test Chat API (using PowerShell):
      $body = @{
          message = "What's the current disease surveillance status?"
          user_id = "test_user"
      } | ConvertTo-Json
      
      Invoke-RestMethod -Uri http://localhost:8000/api/chat -Method Post -Body $body -ContentType "application/json"
   
   4. Get Anomalies:
      curl http://localhost:8000/api/anomalies?days=7
   
   5. Get Predictions:
      curl http://localhost:8000/api/predictions?days=30

B. Test Frontend
   ─────────────────────────────────────────
   
   1. Open Chat Interface:
      - Go to http://localhost:3000
      - Type: "Show me current surveillance status"
      - Wait for agent response
   
   2. Check Dashboard:
      - Go to http://localhost:3000/dashboard
      - Verify metrics are loading
      - Check data source status
   
   3. View Anomalies:
      - Go to http://localhost:3000/anomalies
      - Apply filters
      - Check visualizations
   
   4. View Predictions:
      - Go to http://localhost:3000/predictions
      - Filter by disease/region
      - Check charts

C. Test Multi-Agent Workflow
   ─────────────────────────────────────────
   
   Test queries to trigger different agent flows:
   
   1. General Query (Assistant Agent only):
      "What is this system about?"
   
   2. Anomaly Detection Flow:
      "Are there any unusual health patterns in Mumbai?"
   
   3. Prediction Flow:
      "Predict influenza spread for the next 3 weeks"
   
   4. Full Surveillance Flow:
      "Conduct complete outbreak risk assessment for Delhi region"
   
   5. Alert Generation:
      "Generate health alerts for detected anomalies"

D. Test Thinking Logs
   ─────────────────────────────────────────
   
   1. Send a query via chat interface
   2. Copy the session ID from response
   3. Go to http://localhost:3000/thinking-logs
   4. Paste session ID
   5. View transparent agent reasoning

E. Test Using Postman/Insomnia
   ─────────────────────────────────────────
   
   Import this API collection:
   
   POST http://localhost:8000/api/chat
   Headers: Content-Type: application/json
   Body:
   {
       "message": "Analyze current disease outbreak risks",
       "user_id": "test_user"
   }

═══════════════════════════════════════════════════════════════════════════════
  9. TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Problem: Backend fails to start
──────────────────────────────────────────────
❌ Error: "ValueError: Azure AI Project connection string is not configured"
✓ Solution: Check backend/.env file has correct AZURE_AI_AGENT_PROJECT_CONNECTION_STRING

❌ Error: "Database connection failed"
✓ Solution: 
  - Verify DB_CONNECTION_STRING in .env
  - Check Azure SQL firewall allows your IP
  - Test connection: python main.py --test-connection

❌ Error: "ODBC Driver not found"
✓ Solution:
  - Install ODBC Driver 17 or 18 for SQL Server
  - Update connection string to match installed driver version

❌ Error: "Module not found" errors
✓ Solution:
  - Activate virtual environment
  - Reinstall dependencies: uv pip install -r requirements.txt

Problem: Frontend fails to start
──────────────────────────────────────────────
❌ Error: "Module not found" or "Cannot find module"
✓ Solution:
  - Delete node_modules and package-lock.json
  - Run: npm install

❌ Error: "Port 3000 already in use"
✓ Solution:
  - Kill process: npx kill-port 3000
  - Or use different port: npm run dev -- -p 3001

❌ Error: "Failed to fetch" in frontend
✓ Solution:
  - Verify backend is running on http://localhost:8000
  - Check NEXT_PUBLIC_API_URL in frontend/.env.local
  - Check browser console for CORS errors

Problem: API returns errors
──────────────────────────────────────────────
❌ Error: "Failed to get status" or 500 errors
✓ Solution:
  - Check backend logs for detailed error
  - Verify database connection
  - Ensure all tables are created
  - Check Azure AI Project is accessible

❌ Error: "Agent creation failed"
✓ Solution:
  - Verify model deployment name is correct
  - Check Azure AI Project connection
  - Ensure sufficient quota/credits
  - Review Azure AI Project status

❌ Error: "Bing tool initialization failed"
✓ Solution:
  - Bing is optional, system works without it
  - Verify BING_SEARCH_API_KEY if using
  - Check BING_CONNECTION_NAME in AI Project

Problem: Database issues
──────────────────────────────────────────────
❌ Error: "Invalid object name" or table not found
✓ Solution:
  - Run create_surveillance_tables.sql script
  - Verify tables exist: SELECT * FROM INFORMATION_SCHEMA.TABLES

❌ Error: "Login failed for user"
✓ Solution:
  - Check username/password in connection string
  - Verify SQL authentication is enabled
  - Check Azure SQL firewall rules

Problem: Slow performance
──────────────────────────────────────────────
✓ Solutions:
  - Upgrade Azure SQL tier
  - Add database indexes (already in schema)
  - Use Azure AI Project in same region as other resources
  - Check network latency
  - Monitor agent timeout settings in config/settings.py

Problem: No data showing in dashboard
──────────────────────────────────────────────
✓ Solutions:
  - Run sample_data.sql to populate test data
  - Check data_sources endpoint: http://localhost:8000/api/data-sources
  - Verify backend is connected to correct database
  - Check browser console for errors

Getting Help:
──────────────────────────────────────────────
1. Check logs:
   - Backend: Terminal output where main.py is running
   - Frontend: Browser console (F12)
   - Database: Azure Portal → SQL Database → Query editor

2. Enable debug logging:
   - Backend: Set LOG_LEVEL=DEBUG in .env
   - Restart backend

3. Review documentation:
   - README.md
   - API docs: http://localhost:8000/docs
   - Azure AI documentation

═══════════════════════════════════════════════════════════════════════════════
  10. ARCHITECTURE OVERVIEW
═══════════════════════════════════════════════════════════════════════════════

System Flow:
────────────────────────────────────────────────────────────

User Request → Frontend (Next.js) → Backend API (FastAPI) → Agent Manager
                                                               ↓
                            ┌──────────────────────────────────┴────────┐
                            ↓                                           ↓
                    Surveillance Manager                         Database
                            ↓                                           ↑
                    ┌───────┴────────┐                                 │
                    ↓                ↓                                  │
            AgentGroupChat    Azure AI Agents                          │
                    ↓                                                   │
        ┌───────────┴──────────────┐                                  │
        ↓           ↓               ↓                                  │
  Data Collection  Anomaly    Prediction Agent                        │
     Agent       Detection         ↓                                   │
        ↓         Agent       Alert Agent                              │
        ↓            ↓              ↓                                   │
        └────────────┴──────────────┴→ Reporting Agent ────────────────┘
                                        ↓
                                   Response to User

Agent Workflow Examples:
────────────────────────────────────────────────────────────

1. Simple Status Query:
   User → Assistant Agent → Response

2. Anomaly Detection:
   User → Data Collection → Anomaly Detection → Reporting → Response

3. Outbreak Prediction:
   User → Data Collection → Anomaly Detection → Prediction → Reporting → Response

4. Full Surveillance:
   User → Data Collection → Anomaly Detection → Prediction → Alert → Reporting → Response

Key Components:
────────────────────────────────────────────────────────────

Backend:
  - FastAPI: REST API server
  - Semantic Kernel: Agent orchestration framework
  - Azure AI Agent Service: Multi-agent coordination
  - Plugins: Specialized functions for each agent
  - Managers: Surveillance and session management
  - Models: ML models for anomaly detection

Frontend:
  - Next.js: React framework with SSR
  - React: UI components
  - Tailwind CSS: Styling
  - Plotly/Chart.js: Data visualizations

Database:
  - Azure SQL: Relational database
  - 10 tables for surveillance data, alerts, predictions, logs

Azure Services:
  - Azure AI Foundry: Agent infrastructure
  - Azure OpenAI: Language models
  - Azure Storage: Report storage
  - Bing Search: Real-time information grounding

═══════════════════════════════════════════════════════════════════════════════
  QUICK REFERENCE - COMMON COMMANDS
═══════════════════════════════════════════════════════════════════════════════

Backend:
  cd disease-surveillance-ai/backend
  .\.venv\Scripts\activate                # Activate venv
  uv pip install -r requirements.txt      # Install dependencies
  python main.py                          # Start API server
  python main.py --test-connection        # Test database
  streamlit run streamlit_app.py          # Start dashboard

Frontend:
  cd disease-surveillance-ai/frontend
  npm install                             # Install dependencies
  npm run dev                             # Start dev server
  npm run build                           # Production build
  npm start                               # Start production server

URLs:
  Backend API: http://localhost:8000
  API Docs: http://localhost:8000/docs
  Frontend: http://localhost:3000
  Streamlit: http://localhost:8501

═══════════════════════════════════════════════════════════════════════════════
  PRODUCTION DEPLOYMENT NOTES
═══════════════════════════════════════════════════════════════════════════════

For production deployment:

1. Backend:
   - Use gunicorn or uvicorn with multiple workers
   - Set environment variables in Azure App Service
   - Enable Application Insights for monitoring
   - Use Azure SQL in production tier
   - Enable SSL/TLS

2. Frontend:
   - Build: npm run build
   - Deploy to Azure Static Web Apps or App Service
   - Update NEXT_PUBLIC_API_URL to production backend
   - Enable CDN for static assets

3. Security:
   - Use Azure Key Vault for secrets
   - Enable Managed Identity
   - Configure Azure Firewall
   - Implement rate limiting
   - Enable authentication/authorization

4. Monitoring:
   - Azure Application Insights
   - Log Analytics
   - Health check endpoints
   - Agent thinking logs

═══════════════════════════════════════════════════════════════════════════════

🎉 Setup Complete! Your Disease Surveillance AI Agent System is ready to use.

For questions or issues, check:
  - README.md for detailed documentation
  - http://localhost:8000/docs for API documentation
  - GitHub issues for community support

═══════════════════════════════════════════════════════════════════════════════
